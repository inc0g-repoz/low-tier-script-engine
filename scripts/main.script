include "math.script";
include "util.script";

TEST = no_instance("test.Test").TEST_OBJECT;

function test_async() {
    for (i = 1; i <= 10; i++) {
        println("Hello from a different thread! (x" + i + ")");
    }
}

function main() {
    async(this::test_async);
    EXECUTOR_SERVICE.shutdown();

    n = 5;
    println("Factorial " + n + " = " + factorial_r(n));
}
