// var a = -0.5 + 0.3;

/*
function foo(e) {
	d = e.call(2) ^ 4;
	d++;
	e.logger.info(d++);
	e.logger(e.toString());

	bool = (false && 1 == 2) || !true;
	e.logger.info(bool);

	while (i < 2) {}

	for (var j = 1; j <= 2; j = j + 1) {
		e.logger.info(j);
	}

	if (!true) e.logger.info("true");
	else e.logger.info("false");

	arr = e.arr[0];
	e.logger.info(arr);
}
*/

function test_arithmetic() {
    a = 32 + (8 * 4) * 6 / 2;
    return a;
}

function test_rv(a) {
    boolean = 1 == 1;

    if (boolean) {
        return "Condition Met";
    } else {
        string = ("test" + "ed");
        return "toa" + string.substring(2);
    }
}

function test_loop(a) {
    /*
    boolean = 1 == 1;

    if (boolean) {
        return "Condition Met";
    } else {
        string = ("test" + "ed");
        return "toa" + string.substring(2);
    }
    */

    a = "";
    while (a.length() != 10) {
        a = a + "1";
    }
    return a;
}

/*
function test_condition() {
    a = 1;
    if (a == 2) {
       return "two";
    } else if (a == 3) {
       return "three";
    } else if (a == 1) {
       return "one"
    }
    return "four";
}
*/

function test(arg0) {
    arg0.field = 1;
    return arg0;
}
